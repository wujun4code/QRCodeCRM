<!DOCTYPE html>
<html>

<head>
    <title>扫码有优惠！</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="stylesheet" href="http://cdn.bootcss.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel="stylesheet" href="http://cdn.bootcss.com/bootstrap/3.3.4/css/bootstrap-theme.min.css">
    <script type="text/javascript" src="http://upcdn.b0.upaiyun.com/libs/jquery/jquery-2.0.3.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
</head>

<body>
    <div class="form-horizontal">
        <form id="salesman_form" class="form-horizontal" autocomplete="on" method="POST">
            <div class="form-group">
                <label class="col-sm-2 control-label">请输入您的手机号：</label>
                <div class="col-sm-10">
                    <input id="text" type="text" class="form-control" name="client_mobileNumber" autocomplete="on" placeholder="手机号"/>
                    <input name="salesman_id" autocomplete="on" placeholder="手机号" hidden="hidden" value= <%= salesmanId %> />
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-12">
                    <button id="bind" class="col-sm-2 col-md-offset-2 btn btn-primary" data-loading-text="提交中……" autocomplete="off">提交</button>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-2 col-md-offset-2" id="client_info"></div>
            </div>
        </form>
    </div>

    <script type="text/javascript">
        $("#bind").click(function () {
            var btn = $(this);
            btn.button("loading");
            var url = "/bind"; // the script where you handle the form input.
            $.ajax({
                type: "POST",
                url: url,
                data: $("#salesman_form").serialize(),
                success: function (data) {
                    alert(data.message);
                    btn.button('reset');
                }
            });
            return false; // avoid to execute the actual submit of the form.
        });
    </script>
</body>